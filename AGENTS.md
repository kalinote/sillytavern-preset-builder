# 项目目标

SillyTavern Preset Studio 旨在为 SillyTavern Chat Completion 预设提供安全、直观、工程化的管理与可视化的编辑体验。

- 将单一 Preset JSON 拆分为易维护的 Prompt、Regex、Script 等工程文件，并可可靠重建。
- 提供预设的编辑、校验、预览、版本留存与导入导出能力，保障数据完整性和兼容性。
- 安全连接已有 SillyTavern，在用户明确确认后读取或推送预设，避免意外覆盖与敏感信息泄露。

## 开发约束

1. 不擅自增加环境变量配置。确有必要时，必须先征得用户同意；添加后必须提供合理的默认值。
2. 避免添加过度的边界检查和测试。本项目是个人使用的简单工具，不按大型商业项目或公开服务的标准过度加固。
3. 保持代码简洁、直接，避免过度设计和不必要的抽象。
